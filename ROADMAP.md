# Chancer — Roadmap

AI-помощник для прогнозирования ставок на CS-матчи. Сбор данных → аналитика → прогнозирование → чат-интерфейс.

---

## Статус задач

| # | Issue | Название | Статус |
|---|-------|----------|--------|
| 001 | — | Схема БД + скраппер команд (Teams) | ✅ Готово |
| 002 | — | Загрузка матчей (HLTV-скраппер) | 🅿 На паузе |
| 003 | — | Устойчивый скраппинг HLTV (анти-бот) | 🅿 На паузе |
| 004 | — | Интеграция PandaScore: команды + матчи | ✅ Готово |
| 005 | — | Расширение данных PandaScore (карты, детали матча, состав) | ✅ Готово |
| 006 | — | Агрегация новостей и трансферов | 🗂 Идея |
| 007 | — | Аналитика формы команды | 🗂 Идея |
| 008 | — | H2H-сравнение команд | 🗂 Идея |
| 009 | — | Генерация прогноза на матч (AI) | 🗂 Идея |
| 010 | — | Чат-интерфейс (free-form запросы) | 🗂 Идея |
| 011 | — | Лента предстоящих матчей + быстрый прогноз | 🗂 Идея |

**Легенда:** ✅ Готово · 📋 Запланировано (есть spec) · 🗂 Идея · 🅿 На паузе

---

## Текущий фокус

**Задачи 004–005 завершены.** Следующий шаг — аналитика: задача 007 (форма команды), 008 (H2H), 009 (прогноз).

> Задачи 002 и 003 (HLTV) на паузе. PandaScore — основной источник данных.

---

## Архитектурные решения

### Источник данных: PandaScore вместо HLTV

**Решение (2026-04-04):** PandaScore предоставляет структурированный REST API для CS2. Это исключает необходимость в Selenium, обходе анти-бот защиты и batch-инфраструктуре.

Ключевые эндпоинты:

| Эндпоинт | Назначение |
|----------|------------|
| `GET /csgo/teams` | Список команд с рейтингом |
| `GET /csgo/matches/past` | История матчей (фильтр по команде, дате) |
| `GET /csgo/matches/upcoming` | Предстоящие матчи |
| `GET /csgo/matches/{id}` | Детали матча + карты |
| `GET /csgo/players/{id}/stats` | Статистика игрока |
| `GET /tournaments/{id}/rosters` | Составы команд |

Все запросы с `filter[videogame_title]=cs-2`.

### Схема БД: добавление PandaScore ID

**Решение (2026-04-04):** Не переименовывать существующие `hltv_id`-поля, а добавить `pandascore_id` как новый столбец там, где нужно.

Изменения схемы в рамках 004:

| Таблица | Добавить | Примечание |
|---------|----------|------------|
| `teams` | `pandascore_id` (integer, unique) | Рядом с существующим `hltv_id` |
| `teams` | `pandascore_rank` (integer) | Рядом с `hltv_rank` |
| `matches` | `pandascore_id` (integer, unique) | Ключ дедупликации |
| `players` | `pandascore_id` (integer, unique) | Задача 005 |

Стратегия дедупликации: upsert по `pandascore_id`. Команды-оппоненты, отсутствующие в БД, создаются как минимальные записи (`pandascore_id` + `name`).

### Детальная статистика игроков (задача 005)

Статистика по картам (kills/deaths/rating) требует отдельного запроса на каждый матч — `GET /csgo/matches/{id}/games/{game_id}`. Начинаем без неё: сначала матчи + счёт по картам, player_stats — отдельная задача 005.

---

## Открытые вопросы

- [ ] Удалить колонки `matches.played_at` и `matches.tournament` после мёржа задач 007–009 (временно сохранены для обратной совместимости — см. spec 005).
- [ ] Нужен ли отдельный эндпоинт для upcoming matches, или задача 011 покрывает это?
- [ ] Какая модель AI используется для прогнозирования (задача 009) — внешний LLM или собственная ML-модель?
- [ ] Нужна ли авторизация?

---

## Ограничения и допущения

- Авторизация — вне текущего scope.
- Новые гемы — только по явному согласованию.
- Существующие миграции не трогать.
- PandaScore — основной источник на старте (бесплатный план, 1 000 req/час).
- HLTV — второй источник, подключается позже (задачи 002, 003).
